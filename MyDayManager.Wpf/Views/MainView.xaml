<UserControl x:Class="MyDayManager.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <materialDesign:ColorZone materialDesign:ShadowAssist.ShadowDepth="Depth3" Mode="PrimaryDark">
            <Grid>
                <TextBlock FontSize="65" FontFamily="../Fonts/#Dulcelin" Margin="30" HorizontalAlignment="Center" VerticalAlignment="Center" Text="MyDay Manager"/>
            </Grid>
        </materialDesign:ColorZone>
    </Grid>
</UserControl>
