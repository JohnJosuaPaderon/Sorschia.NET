<UserControl x:Class="Sorschia.DevTeam.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Margin="50 0" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Source="../Images/logo.png" Height="300" Width="300"/>
        <Grid Grid.Column="2" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Welcome to Sorschia" Style="{DynamicResource MaterialDesignDisplay2TextBlock}"/>
            <TextBlock Grid.Row="2" Text="Dev-Team" Style="{DynamicResource MaterialDesignDisplay4TextBlock}" FontWeight="Bold"/>
        </Grid>
    </Grid>
</UserControl>
